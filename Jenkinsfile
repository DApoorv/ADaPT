pipeline {
         agent any
         stages {
                 stage('Build') {
                 steps {
                     sh 'ls -lah'
					 sh 'chmod +x ./build.sh'
					 sh './build.sh'
                       }
					}
			}
		}